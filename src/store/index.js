import Vue from 'vue'
import Vuex from 'vuex'
import userStatus from './StoreUserStatus'

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    userStatus
  }
})
